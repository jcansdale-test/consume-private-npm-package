on:
  push:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
    - run: npm i https://foo:${{ secrets.PAT }}@github.com/jcansdale/private-npm-package
    - run: npx jcansdale/private-npm-package#1.0.2               # execute the package
